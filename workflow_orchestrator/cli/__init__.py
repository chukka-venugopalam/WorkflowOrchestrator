"""CLI layer — Layer 0 entry points for the Workflow Orchestrator.

This package provides:
- Typer-based CLI commands (``workflow`` command)
- Rich interactive menu (backward compat via ``workflow gui``)
- Configuration wizard (future)
"""

from __future__ import annotations
