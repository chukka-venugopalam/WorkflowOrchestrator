"""CLI command implementations.

This package will contain individual command modules:
- ``run.py`` — Execute workflows
- ``config.py`` — Configuration management
- ``plugins.py`` — Plugin management
- ``reports.py`` — Report viewing
- ``schedule.py`` — Workflow scheduling
- ``scan.py`` — Project scanning

Currently, commands are in ``workflow_orchestrator/cli.py``.
They will be migrated into this package in Phase 1.
"""

from __future__ import annotations
